import { EvidenceUploadClient } from "@/components/evidence/EvidenceUploadClient";

export default function Page() {
  return (
    <main className="flex w-full flex-1 flex-col p-8">
      <EvidenceUploadClient />
    </main>
  );
}
